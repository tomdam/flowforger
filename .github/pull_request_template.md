## What and why

<!-- What does this change, and what problem does it solve? Link the issue if there is one (e.g. "Fixes #12"). -->

## How it was tested

<!-- Tests added/updated? If this touches expressions, connector output shapes or emitted JSON: how did you check it matches Power Automate? -->

## Checklist

- [ ] `npm run build` and `npm test` pass
- [ ] If `packages/dsl-native/src/context.ts` changed, `monaco-types.ts` was updated to match
- [ ] No tokens, real tenant URLs, emails or environment GUIDs in code, examples or fixtures
